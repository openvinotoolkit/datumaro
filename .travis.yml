language: python

cache: pip

python:
  - '3.6'
  - '3.7'
  - '3.8'

matrix:
  include:
    - dist: xenial
    - dist: bionic

install:
  - pip install -e ./

  # plugins
  - pip install tensorflow

script:
  - python -m unittest discover -v

  - datum -h