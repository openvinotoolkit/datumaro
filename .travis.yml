language: python

cache: pip

python:
  - '3.6'
  - '3.7'
  - '3.8'

matrix:
  include:
    - dist: xenial

    - dist: bionic
      python: '3.6'
    - dist: bionic
      python: '3.7'
    - dist: bionic
      python: '3.8'

install:
  - pip install -e ./

  # plugins
  - pip install tensorflow

script:
  - python -m unittest discover -v

  - datum -h